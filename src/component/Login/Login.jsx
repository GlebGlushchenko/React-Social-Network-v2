import React from 'react';

const Login = () => {
  return (
    <div>
      <h1>LOGIN</h1>
      <h2>Login</h2>
      <input defaultValue="login" placeholder={'Login'} type="text" />
      <h2>Passward</h2>
      <input defaultValue="passward" placeholder={'Passward'} type="text" />
    </div>
  );
};

export default Login;
